export const environment = {
  production: true,
  /**
   * Production API URL (HTTPS). Change before `ng build` or override via build-time replace.
   * Example: https://api.yourdomain.com/api — must match backend public URL + /api
   */
  apiBaseUrl: 'https://backend-project-job.onrender.com/api'
};
